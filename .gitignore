# Client course content — never commit.
# Source .docx files, their unzipped contents, parsed JSON and generated documents.
work/
dist/
*.docx
*.doc
*.pdf
*.xlsx

# Exception: Coursera's own blank Course Template is a build dependency, not course content.
# Checked for personal metadata before committing — no docProps, empty comment authors, empty
# threaded-comment person list.
!templates/*.xlsx

# Parsed intermediates
quiz.json
outline.json
course.json
*.lines.txt
*.clean.txt

node_modules/
.DS_Store
Thumbs.db
